CREATE TABLE example (
  id CHAR(26) NOT NULL,
  name TEXT DEFAULT NULL, -- sanitize-html
  age INT DEFAULT 0,
  created_at DATETIME DEFAULT NULL,
  updated_at DATETIME DEFAULT NULL,
  deleted_at DATETIME DEFAULT NULL,
  PRIMARY KEY (id),
  KEY idx_id (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
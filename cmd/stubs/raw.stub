package models

import "github.com/not-empty/grit/app/helper"

func init() {
	helper.RegisterRawQueries("{{.DomainLower}}", map[string]string{
		"count": `
      SELECT
        COUNT(1) as total
      FROM {{.DomainLower}}
    `,
	})
}
